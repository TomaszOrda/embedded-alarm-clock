[package]
edition      = "2024"
name         = "embedded-alarm-clock"
rust-version = "1.88"
version      = "0.1.0"

[[bin]]
name = "embedded-alarm-clock"
path = "./src/bin/main.rs"

[dependencies]
esp-hal = { version = "~1.1.0", features = ["esp32c3", "log-04", "unstable"] }


esp-bootloader-esp-idf = { version = "0.5.0", features = ["esp32c3", "log-04"] }
log                    = "0.4.27"

critical-section = "1.2.0"
esp-backtrace = { version = "0.19.0", features = [
  "esp32c3",
  "panic-handler",
  "println",
] }
esp-println = { version = "0.17.0", features = ["esp32c3", "log-04"] }
embassy-executor = { version = "0.10.0", features = ["platform-riscv32"] }
embassy-futures = "0.1.2"
embassy-time = "0.5.1"
embedded-graphics = "0.8.2"
embedded-hal-bus = "0.3.0"
esp-rtos = { version = "0.3.0", features = ["esp32c3", "embassy"] }
ds323x = "0.7.0"
epd-waveshare = "0.6.0"
heapless = "0.9.3"
crc = "3.4.0"
u8g2-fonts = { version = "0.8", features = ["embedded_graphics_textstyle"] }


# For fine tuning these settings, please refer to https://doc.rust-lang.org/cargo/reference/profiles.html
[profile.dev]
# The default debug profile is too slow and too big for resource-constrained devices.
# Always build with some optimizations enabled.
opt-level = "s"

[profile.release]
codegen-units = 1     # LLVM can perform better optimizations using a single thread
debug         = 2     # prefer slower builds but better debugging experience
lto           = 'fat'
opt-level     = 's'
